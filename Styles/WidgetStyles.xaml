<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <!-- Typography and Foreground Tokens -->
    <SolidColorBrush x:Key="WidgetTextBrush" Color="#EEFFFFFF" />
    <SolidColorBrush x:Key="WidgetThinkingTextBrush" Color="#B3FFFFFF" />
    <SolidColorBrush x:Key="WidgetErrorTextBrush" Color="#FFFF8A8A" />
    <SolidColorBrush x:Key="WidgetIconBrush" Color="#99FFFFFF" />

    <!-- Material UI Tokens (Floating Command Bar) -->
    <SolidColorBrush x:Key="WidgetBackgroundBrush" Color="#E61A1B23" />
    <SolidColorBrush x:Key="WidgetBorderBrush" Color="#1AFFFFFF" />
    <SolidColorBrush x:Key="WidgetHoverBorderBrush" Color="#33FFFFFF" />
    <SolidColorBrush x:Key="WidgetDragHandleBrush" Color="#15FFFFFF" />
    <SolidColorBrush x:Key="WidgetRippleBrush" Color="#15FFFFFF" />

    <!-- Active State Highlight (Subtle glowing line on active/thinking state) -->
    <LinearGradientBrush x:Key="WidgetActiveHighlightBrush" StartPoint="0,0.5" EndPoint="1,0.5">
        <GradientStop Color="#0060A5FA" Offset="0" />
        <GradientStop Color="#FF3B82F6" Offset="0.5" />
        <GradientStop Color="#0060A5FA" Offset="1" />
    </LinearGradientBrush>

    <!-- Typography Styles -->
    <Style x:Key="WidgetThinkingTextStyle" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="Segoe UI Variable Display" />
        <Setter Property="FontSize" Value="15" />
        <Setter Property="FontWeight" Value="SemiLight" />
        <Setter Property="Foreground" Value="{StaticResource WidgetThinkingTextBrush}" />
        <Setter Property="LineHeight" Value="20" />
        <Setter Property="MaxLines" Value="1" />
        <Setter Property="TextTrimming" Value="CharacterEllipsis" />
    </Style>

    <Style x:Key="WidgetResultTextStyle" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="Segoe UI Variable Text" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="FontWeight" Value="Normal" />
        <Setter Property="Foreground" Value="{StaticResource WidgetTextBrush}" />
        <Setter Property="LineHeight" Value="20" />
        <Setter Property="MaxLines" Value="3" />
        <Setter Property="TextTrimming" Value="CharacterEllipsis" />
        <Setter Property="TextWrapping" Value="Wrap" />
    </Style>

    <!-- Subtle Answer Button (Interactive Text Only) -->
    <Style x:Key="WidgetAnswerButtonStyle" TargetType="Button">
        <Setter Property="FontFamily" Value="Segoe UI Variable Display" />
        <Setter Property="FontSize" Value="15" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Foreground" Value="{StaticResource WidgetTextBrush}" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderBrush" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Padding" Value="16,8" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Grid Background="Transparent">
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="CommonStates">
                                <VisualState x:Name="Normal" />
                                <VisualState x:Name="PointerOver">
                                    <Storyboard>
                                        <DoubleAnimation
                                            Storyboard.TargetName="ContentPresenter"
                                            Storyboard.TargetProperty="Opacity"
                                            To="0.8"
                                            Duration="0:0:0.1" />
                                    </Storyboard>
                                </VisualState>
                                <VisualState x:Name="Pressed">
                                    <Storyboard>
                                        <DoubleAnimation
                                            Storyboard.TargetName="ContentPresenter"
                                            Storyboard.TargetProperty="Opacity"
                                            To="0.6"
                                            Duration="0:0:0.05" />
                                        <DoubleAnimation
                                            Storyboard.TargetName="ContentPresenterTransform"
                                            Storyboard.TargetProperty="ScaleX"
                                            To="0.96"
                                            Duration="0:0:0.05" />
                                        <DoubleAnimation
                                            Storyboard.TargetName="ContentPresenterTransform"
                                            Storyboard.TargetProperty="ScaleY"
                                            To="0.96"
                                            Duration="0:0:0.05" />
                                    </Storyboard>
                                </VisualState>
                                <VisualState x:Name="Disabled">
                                    <Storyboard>
                                        <DoubleAnimation
                                            Storyboard.TargetName="ContentPresenter"
                                            Storyboard.TargetProperty="Opacity"
                                            To="0.4"
                                            Duration="0" />
                                    </Storyboard>
                                </VisualState>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>

                        <ContentPresenter
                            x:Name="ContentPresenter"
                            Content="{TemplateBinding Content}"
                            ContentTemplate="{TemplateBinding ContentTemplate}"
                            HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                            Padding="{TemplateBinding Padding}"
                            VerticalAlignment="{TemplateBinding VerticalContentAlignment}">
                            <ContentPresenter.RenderTransform>
                                <ScaleTransform x:Name="ContentPresenterTransform" CenterX="20" CenterY="10" />
                            </ContentPresenter.RenderTransform>
                        </ContentPresenter>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
